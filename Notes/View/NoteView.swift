//

import SwiftUI

struct NoteView: View {
    var body: some View {
        Text("Note")
    }
}

struct NoteView_Previews: PreviewProvider {
    static var previews: some View {
        NoteView()
    }
}
